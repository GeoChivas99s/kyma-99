import React from 'react';
import {StyleSheet, Text, SafeAreaView} from 'react-native';

const ExecercicePeople = () => {
  return (
    <SafeAreaView>
      <Text>ExecercicePeople</Text>
    </SafeAreaView>
  );
};

export default ExecercicePeople;

const styles = StyleSheet.create({});
